<% provide(:title, 'レシピ一覧') %>

<%= render 'layouts/hero', hero_title: 'レシピ一覧' %>

<section class="section">
  <div class="container">
    <div class="columns is-multiline is-mobile" id="recipe-container">
      <%= render @recipes %>
    </div>
  </div>
</section>
