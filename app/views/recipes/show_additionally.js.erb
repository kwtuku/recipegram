infiniteScrollContainer.insertAdjacentHTML('beforeend', "<%= j(render partial: @file_path, collection: @items) %>")
<% if @items.present? %>
  infiniteScrollContainer.dataset.infiniteScroll = 'true';
<% end %>
