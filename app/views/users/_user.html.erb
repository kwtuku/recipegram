<div class="column is-12-mobile is-6-tablet is-4-desktop p-0-mobile">
  <div class="card" data-infinite-scroll-item="">
    <div class="card-content">
      <div class="media">
        <figure class="media-left">
          <p class="image is-64x64">
            <%= link_to user_path(user) do %>
              <%= image_tag user.user_image.url(:icon), class: 'is-rounded' %>
            <% end %>
          </p>
        </figure>
        <div class="media-content">
          <p class="is-size-5 is-1-line">
            <span><%= link_to user.nickname, user_path(user), title: user.nickname, class: 'has-text-black-bis text-break hover-underline' %></span>
          </p>
          <p class="has-text-grey-light mb-2">@<%= user.username %></p>
          <%= render 'shared/follow_button', user: user, size: 'is-small' %>
        </div>
      </div>
      <% if user.following?(current_user) %>
        <div class="content">
          <span class="tag is-info is-light">フォローされています</span>
        </div>
      <% end %>
    </div>
  </div>
</div>
